# Python
__pycache__/
*.py[cod]
*.so
*.egg-info/
build/
dist/

# uv / venv
.venv/

# IDE / OS
.vscode/
.idea/
.DS_Store

# Test / lint
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Secrets
.env
.env.*
!.env.example

# Local data
data/
*.log
*.wav
*.mp3

# Per-user eval baseline (created by `voice-eval baseline --save`).
# The headline scores at evals/scores.json *are* tracked — only the
# user-local regression baseline is ignored.
evals/baseline.json
